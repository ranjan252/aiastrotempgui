import React from 'react';

const ComponentName = () => {
    return (
        <div>
            <h2>This is the {ComponentName} component.</h2>
        </div>
    );
};

export default ComponentName;